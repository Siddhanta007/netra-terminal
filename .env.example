# Browser-visible Vite configuration. Leave VITE_API_URL empty on Vercel so
# requests use the same-origin /api proxy. Local development can point directly
# to the local FastAPI server.
VITE_API_URL=http://localhost:7860
VITE_GOOGLE_CLIENT_ID=

# Server-only Vercel configuration. Never prefix secrets with VITE_.
HF_SPACE_URL=https://siddhanta007-netra-server.hf.space
HF_TOKEN=
